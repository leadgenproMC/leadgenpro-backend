services:
  - type: web
    name: leadgenpro-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    pythonVersion: 3.11.0
    envGroup: evg-d806khho3t8c73dg66og
    healthCheckPath: /healthz
